pipeline {
    agent any

    stages {
        stage('without docker') {
            steps {
                sh ''' 
                    echo "Hello World"
                '''
            }
        }

    }
}
